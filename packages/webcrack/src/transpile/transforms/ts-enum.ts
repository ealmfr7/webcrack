// Placeholder: TypeScript enum IIFE to enum transform (implemented in a later task).
import type { Transform } from '../../ast-utils';

export default {
  name: 'ts-enum',
  tags: ['unsafe'],
  scope: true,
  visitor() {
    return {};
  },
} satisfies Transform;
