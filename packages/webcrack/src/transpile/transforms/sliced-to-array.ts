// Placeholder: _slicedToArray to destructuring transform (implemented in a later task).
import type { Transform } from '../../ast-utils';

export default {
  name: 'sliced-to-array',
  tags: ['unsafe'],
  scope: true,
  visitor() {
    return {};
  },
} satisfies Transform;
