// Placeholder: _toConsumableArray / _objectSpread to spread transform (implemented in a later task).
import type { Transform } from '../../ast-utils';

export default {
  name: 'spread-helpers',
  tags: ['unsafe'],
  scope: true,
  visitor() {
    return {};
  },
} satisfies Transform;
