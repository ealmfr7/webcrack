// Placeholder: Dean Edwards packer eval(function(p,a,c,k,e,d){...}) unwrapper (implemented in a later task).
import type { Transform } from '../ast-utils';

export default {
  name: 'packer',
  tags: ['unsafe'],
  scope: true,
  visitor() {
    return {};
  },
} satisfies Transform;
